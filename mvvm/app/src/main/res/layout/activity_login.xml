<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <variable
            name="loginviewmodel"
            type="com.example.mvvm.viewmodel.viewmodellogin" />

    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        tools:context=".view.login">
        <TextView
            android:layout_marginTop="70dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CHÀO MỪNG BẠN"
            android:layout_gravity="center"
            android:textSize="30dp"
            android:textStyle="bold"
            android:textColor="@color/black"
            />
        <TextView
            android:textSize="20dp"
            android:layout_gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:text="Đăng nhập để sử dụng"/>

        <ImageView

            android:layout_marginTop="20dp"
            android:layout_width="200dp"
            android:layout_height="100dp"
            android:layout_gravity="center"
            android:src="@drawable/lobster"
            />
        <EditText
            android:id="@+id/txtemail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Tài khoản"
            android:drawableLeft="@drawable/ic_user"
            android:text="@={loginviewmodel.email}"
            android:letterSpacing="0.6"
            />
        <EditText
            android:drawableLeft="@drawable/ic_padlock"
            android:layout_marginTop="10dp"
            android:id="@+id/txtpass"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={loginviewmodel.pasword}"
            android:letterSpacing="0.6"
            android:hint="Mật khẩu"
            />

        <Button
            android:layout_width="350dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:backgroundTint="#FF5722"
            android:text="ĐĂNG NHẬP"
            android:textSize="20dp"
            android:textStyle="bold" />

        <Button
            android:layout_gravity="center"
            android:layout_width="350dp"
            android:layout_height="wrap_content"
            android:text="ĐĂNG NHẬP BẰNG FACEBOOK"
            android:textStyle="bold"
            android:onClick="@{()->loginviewmodel.onlick()}"
            android:textSize="19dp"
            android:backgroundTint="#1735FA"/>







    </LinearLayout>

</layout>